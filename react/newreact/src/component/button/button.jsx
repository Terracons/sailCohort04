const Button = (props)=>{
    return(
        <div>
            <div >
            <button className= {`${props.class

                
            }`}>{props.text}</button>
           </div>

        </div>
    )

}
export default Button;